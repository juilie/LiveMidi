export const config = {
    name: 'fallingFinal',
    description: 'fallingFinal',
    functions: {
        notePlayed: {
            parameters: {
            }
        }
    }
}

export default config;